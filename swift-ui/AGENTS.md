# swift-ui/RedB/

macOS SwiftUI client — three-column DB browser (sidebar | tables | SQL query).

## DESIGN PRINCIPLES (APPLE HIG — MANDATORY)

- **macOS Human Interface Guidelines**: 所有 UI 变更必须符合 Apple HIG。参考 App：Finder、Xcode、Mail、Safari。
- **No iOS patterns**: 禁止 `.cornerRadius` 行高亮、pill badge、圆角卡片等 iOS 风格元素。macOS 使用平面高亮 + 文字标记。
- **System icons**: 展开/折叠 = `chevron.right`（旋转），文件浏览 = `folder`，执行 = `play`。禁止用 `folder` 图标做展开折叠。
- **System colors only**: `.green`/`.red`/`.orange` 用于语义，`.accentColor`/`.secondary`/`.tertiary` 用于层级。禁止 `Color(red:green:blue:)`。
- **Pure-icon buttons MUST have tooltip**: Toolbar 和 `.borderless` 按钮用原生 `NSView.toolTip`（`.tooltip()` modifier from `Models/View+Tooltip.swift`），不要用 `.help()`。
- **Menu indicators visible**: 所有 `Menu` 必须 `.menuIndicator(.visible)`，用户需要知道是可交互菜单。
- **Animation**: `.easeInOut(duration: 0.2)` 用于布局切换，`.transition(.opacity)` 用于内容出现。禁止无动画突变。
- **Cursor correctness**: 编辑器覆盖层上的按钮必须 `.onHover` 切换为 `NSCursor.arrow`，避免 NSTextView I-beam 光标穿透。

## STRUCTURE

```
swift-ui/RedB/
├── RedBApp.swift           # @main entry, ContentView (NavigationSplitView)
├── Generated/
│   └── redb_core.swift     # Auto-generated by uniffi — DO NOT EDIT
├── Services/
│   └── RustBridge.swift     # FFI dispatch: ffiQueue.async + withCheckedThrowingContinuation
├── Models/
│   ├── DatabaseViewModel.swift  # Central app state (connections, tabs, usage)
│   ├── QueryTab.swift           # Tab model: sqlInput, queryLoadState, baseSql
│   ├── SQLContext.swift         # Auto-complete context helpers
│   ├── QueryResultModel.swift   # Result display model
│   └── SavedQuery.swift         # Persisted query model
└── Views/
    ├── SidebarView.swift        # Connection list + add/edit/delete
    ├── TableBrowserView.swift   # Table list in middle column
    ├── SQLQueryView.swift       # Main query editor + results pane
    ├── QueryResultView.swift    # Result table rendering
    ├── TableStructureView.swift # Column metadata display
    ├── CodeEditor.swift         # Custom NSTextView-based editor
    └── Untitled.swift           # Placeholder
```

## WHERE TO LOOK

| Task | File |
|------|------|
| Add a new view/screen | `Views/` — follows existing view pattern |
| Add connection flow | `DatabaseViewModel.swift` (connect/disconnect/refresh) |
| Tweak FFI dispatch | `RustBridge.swift` |
| Add auto-complete logic | `DatabaseViewModel.swift` — tableSuggestions, columnSuggestions, aliasForTable |
| Change query execution | `DatabaseViewModel.swift` — executeQuery, splitSQL |
| Regenerate bindings | Run `scripts/build-rust.sh` from project root |

## KEY TYPES

- **RustBridge**: `@MainActor ObservableObject` — wraps uniffi `DatabaseManager`, publishes `connectionStatus`
- **DatabaseViewModel**: central hub — publishes connections, tables, queryTabs, savedQueries
- **ConnectionProfile**: Swift-side model bridging UI ↔ `DatabaseConfig` (via `toConfig`/`init(from:)`)
- **LoadState\<T\>**: `.idle` / `.loading` / `.success(T)` / `.failure(String)` — used for tables, query results
- **QueryTab**: title + sqlInput + queryLoadState + baseSql — per-tab query state
- **DbType** (enum): sqlite/postgres/mysql/mariaDb/sqlServer/db2 — mirrors `DatabaseType` with `toFFI`

## CONVENTIONS

- All FFI: `withCheckedThrowingContinuation` + `ffiQueue.async` — never call uniffi from main thread
- UserDefaults for `lastConnectionId`, `lastQuerySql` persistence
- `ConnectionStore`/`QueryStore` opened from `ApplicationSupport/RedB/` at startup
- Column suggestions derived from first table in parsed SQL
- Table usage tracked → frequency-sorted auto-complete

## ANTI-PATTERNS

- Generated/redb_core.swift — manually editing will be overwritten on next build
- `bridge.connect()` called from `onChange(of: selectedConnection)` — watch for infinite loops
